<view class="page">
  <view class="logo">管理员登录</view>
  <view class="c-card form">
    <input
      class="form__input"
      placeholder="账号"
      value="{{username}}"
      data-field="username"
      bindinput="onInput"
    />
    <input
      class="form__input"
      placeholder="密码"
      password
      value="{{password}}"
      data-field="password"
      bindinput="onInput"
    />
    <button class="c-btn c-btn--primary c-btn--block" loading="{{loading}}" bindtap="onLogin">
      登录
    </button>
  </view>
</view>
