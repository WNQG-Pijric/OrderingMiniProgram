<view class="page">
  <!-- 顶部：标题 + 个人中心入口 -->
  <view class="header">
    <text class="title">点餐</text>
    <text class="profile-entry" bindtap="goProfile">我的</text>
  </view>

  <!-- 公告位：模块 09 首页顶部展示 -->
  <view class="notice-placeholder"></view>

  <!-- 分类 tab -->
  <scroll-view class="tabs" scroll-x enable-flick>
    <view
      class="tab {{activeCategoryId === 0 ? 'active' : ''}}"
      data-id="0"
      bindtap="onCategoryTap"
    >全部</view>
    <view
      wx:for="{{categories}}"
      wx:key="id"
      class="tab {{activeCategoryId === item.id ? 'active' : ''}}"
      data-id="{{item.id}}"
      bindtap="onCategoryTap"
    >{{item.name}}</view>
  </scroll-view>

  <!-- 菜品列表 -->
  <view class="menu-list">
    <view wx:if="{{loading}}" class="tip">加载中...</view>
    <view wx:elif="{{menus.length === 0}}" class="tip">暂无菜品</view>
    <view
      wx:for="{{menus}}"
      wx:key="id"
      class="menu-item"
      data-id="{{item.id}}"
      bindtap="goDetail"
    >
      <image wx:if="{{item.image}}" class="menu-img" src="{{item.image}}" mode="aspectFill" />
      <view wx:else class="menu-img placeholder"></view>
      <view class="menu-info">
        <text class="menu-name">{{item.name}}</text>
        <text wx:if="{{item.description}}" class="menu-desc">{{item.description}}</text>
        <view class="menu-bottom">
          <text class="menu-price">¥{{item.price}}</text>
          <text wx:if="{{item.isSpec}}" class="menu-spec-tag">多规格</text>
        </view>
      </view>
    </view>
  </view>
</view>
